name: Deploy Backend

on:
  push:
    branches: [ "main" ]
    paths:
      - 'server/**'
      - '.github/workflows/deploy-be.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'
        cache-dependency-path: server/go.sum

    - name: Build Backend
      working-directory: ./server
      run: |
        go mod download
        GOOS=linux GOARCH=amd64 go build -o kantanto-be main.go

    - name: Copy Binary to VPS
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        source: "server/kantanto-be"
        target: "/var/www/kantanto/server"
        strip_components: 1

    - name: Restart Backend Service
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USERNAME }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        script: |
          # Pindah ke folder target
          mkdir -p /var/www/kantanto/server
          cd /var/www/kantanto/server
          
          # Matikan proses backend lama yang berjalan (jika ada)
          pkill -f "./kantanto-be" || true
          
          # Beri hak akses execute
          chmod +x kantanto-be
          
          # Jalankan ulang backend di background (nohup)
          nohup ./kantanto-be > backend.log 2>&1 &
          
          echo "Backend berhasil di-restart!"
